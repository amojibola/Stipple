Those four prompts cover every scenario:

Starting fresh on a new layer - use the briefing plus the status block

Starting fresh mid-layer - use the briefing plus the status block plus the connector text plus the handoff note

Ending a session mid-layer - use the end of session handoff request before closing

Ending a session on a completed layer - use the build log summary request from your other prompt note and update the status block